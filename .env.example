# Comma-separated invite codes for beta access
SIGNALRY_INVITE_CODES=alpha-one,alpha-two

# Secret key for signing auth tokens (random string, keep private)
SIGNALRY_SECRET=change-me-to-a-random-string
